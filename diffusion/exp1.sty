\usepackage{okumacro}
%\usepackage[top=10mm,bottom=5mm,left=10mm,right=10mm]{geometry}
\usepackage[margin=20mm]{geometry}
\usepackage[format=hang,font=small,labelfont=bf,textfont=bf,labelsep=quad,justification=raggedright,singlelinecheck=true]{caption}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{ascmac}
\usepackage{listings, jlisting}%jlisting.styは各自でダウンロードして
                               %ソースと同じディレクトリに置いておきましょう．
\usepackage{graphicx}
\usepackage{color}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{multicol}
\usepackage{arydshln}
%\usepacakge{arydshln}
\usepackage{url}               %urlを貼り付けるためのパッケージ
\usepackage{setspace}          %行間調整用のパッケージ


%%%%% もしも，captionパッケージが内場合には15-24行のコメントを外す
% \makeatletter
% \long\def\@makecaption#1#2{% #1=図表番号、#2=キャプション本文
% \small\bfseries
% \sbox\@tempboxa{#1\hspace{1em}#2}
% \ifdim \wd\@tempboxa >\hsize
% #1 #2\par
% \else
% \hb@xt@\hsize{\hfil\box\@tempboxa\hfil}
% \fi}
% \makeatother

%% プログラムのソースなどのリストを記述するための設定　ここから
\lstset{%
 language={C}, %
 basicstyle={\footnotesize},%
 identifierstyle={\footnotesize},%
 commentstyle={\footnotesize},%
 keywordstyle={\footnotesize\bfseries},%
 ndkeywordstyle={\footnotesize},%
 stringstyle={\footnotesize\ttfamily},%
 numbers={left},%
 frame={tb},%
 tabsize=2,%
 breaklines=true,%
}
% % プログラムのソースなどのリストを記述するための設定　ここまで

